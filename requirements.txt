# ============================================================================
# AgenticX Core Dependencies (lightweight, no torch, installs in seconds)
# ============================================================================
# 基础框架
pydantic>=2.0.0
typing-extensions>=4.0.0
loguru>=0.7.0
rich>=13.0.0
click>=8.0.0
PyYAML>=6.0
jinja2>=3.0.0
python-dotenv>=1.0.0

# 网络 / HTTP
httpx>=0.25.0
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.0.0
websockets>=12.0

# Web 框架
fastapi>=0.100.0
uvicorn>=0.23.0
typer>=0.9.0

# LLM 核心
openai>=1.50.0
litellm>=1.50.0
tiktoken>=0.7.0
tokenizers>=0.19.0
huggingface-hub>=0.24.0

# 核心工具库
docstring-parser>=0.11,<0.12

# 通用工具
ujson>=5.8.0
tqdm>=4.65.0
joblib>=1.3.0
chardet>=5.0.0
psutil>=5.9.0
structlog>=23.1.0

# ============================================================================
# Optional Dependencies (install with extras groups as needed)
# ============================================================================
#
# pip install "agenticx[llm]"        # 额外 LLM 提供者: anthropic, ollama
# pip install "agenticx[memory]"     # 记忆系统: mem0ai, chromadb, qdrant, redis, pymilvus, numpy
# pip install "agenticx[document]"   # 文档处理: PyMuPDF, pypdf, python-docx, python-pptx ...
# pip install "agenticx[ocr]"        # OCR (会拉入 torch ~2GB): easyocr
# pip install "agenticx[graph]"      # 知识图谱: networkx, neo4j, cdlib ...
# pip install "agenticx[data]"       # 数据分析: pandas, numpy, scikit-learn, scipy, matplotlib ...
# pip install "agenticx[monitoring]" # 可观测性: prometheus-client, opentelemetry
# pip install "agenticx[database]"   # 数据库后端: psycopg2, SQLAlchemy, cryptography
# pip install "agenticx[mcp]"        # MCP 协议: mcp
# pip install "agenticx[mqtt]"       # MQTT 协议: asyncio-mqtt
# pip install "agenticx[text]"       # 文本相似度: python-Levenshtein, RapidFuzz
# pip install "agenticx[volcengine]" # 火山引擎 AgentKit
# pip install "agenticx[all]"        # 全部功能 (不含 dev/docs)
#
# pip install "agenticx[dev]"        # 开发工具: pytest, black, flake8 ...
# pip install "agenticx[docs]"       # 文档构建: mkdocs, mkdocs-material ...
