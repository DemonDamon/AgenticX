/** Customer-facing desktop entries intentionally hidden for the enterprise build. */
export const SHOW_DESKTOP_EXTERNAL_IM = false;

/** Keep Graph Runtime/SSE active while hiding its expert-facing God-View entry. */
export const SHOW_DESKTOP_RUN_GRAPH = false;

/** Customer deployments use the Enterprise knowledge service, not a local document brain. */
export const LOCAL_KNOWLEDGE_ENABLED = false;

/** Customer navigation keeps conversations in one focused viewport. */
export const SHOW_DESKTOP_MULTI_PANE = false;
