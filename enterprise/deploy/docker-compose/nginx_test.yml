services:
  nginx-edge:
    image: nginx:1.27-alpine
    container_name: agenticx-nginx-edge
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ../nginx/edge-split.conf:/etc/nginx/nginx.conf:ro