services:
  nginx:
    image: nginx:1.27-alpine
    container_name: agenticx-nginx-prod
    restart: unless-stopped
    depends_on:
      gateway-a:
        condition: service_started
      gateway-b:
        condition: service_started
      web-portal:
        condition: service_started
      admin-console:
        condition: service_started
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ../nginx/gateway.conf:/etc/nginx/nginx.conf:ro
      - ../nginx/certs:/etc/nginx/certs:ro
    networks:
      - agenticx-backbone

  gateway-a:
    image: agenticx-registry.cn-shanghai.cr.aliyuncs.com/agenticx/enterprise-gateway:latest
    container_name: agenticx-gateway-a
    restart: unless-stopped
    user: "0:0"
    environment:
      GATEWAY_PORT: 8088
      DATABASE_DIALECT: ${DATABASE_DIALECT:-postgresql}
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: redis://redis:6379/0
      DEFAULT_TENANT_ID: ${DEFAULT_TENANT_ID}
      AGX_PROVIDER_SECRET_KEY: ${AGX_PROVIDER_SECRET_KEY}
      DEFAULT_DEPT_ID: ${DEFAULT_DEPT_ID}
      AUTH_JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY}
      GATEWAY_CONFIG_PATH: /app/config/policies.yaml
      GATEWAY_QUOTA_CONFIG_FILE: /runtime/admin/quotas.json
      GATEWAY_QUOTA_USAGE_FILE: /runtime/gateway/quota-usage.json
      GATEWAY_POLICY_SNAPSHOT_FILE: /runtime/admin/policy-snapshot.json
      GATEWAY_POLICY_OVERRIDE_FILE: /runtime/admin/policy-overrides.json
    volumes:
      - gateway_runtime:/runtime
      - ../config/policies.yaml:/app/config/policies.yaml:ro
      - ../../plugins:/app/plugins:ro
    networks:
      - agenticx-backbone

  web-portal:
    image: agenticx-registry.cn-shanghai.cr.aliyuncs.com/agenticx/enterprise-web-portal:latest
    container_name: agenticx-web-portal
    restart: unless-stopped
    user: "0:0"
    environment:
      NODE_ENV: production
      DATABASE_DIALECT: ${DATABASE_DIALECT:-postgresql}
      DATABASE_URL: ${DATABASE_URL}
      AGX_PROVIDER_SECRET_KEY: ${AGX_PROVIDER_SECRET_KEY}
      NEXT_PUBLIC_GATEWAY_BASE: http://nginx
      WEB_PORTAL_PUBLIC_BASE_URL: ${WEB_PORTAL_PUBLIC_BASE_URL:-}
      AUTH_JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY}
      AUTH_JWT_PRIVATE_KEY: ${JWT_PRIVATE_KEY}
      DEFAULT_TENANT_ID: ${DEFAULT_TENANT_ID}
      DEFAULT_DEPT_ID: ${DEFAULT_DEPT_ID}
    networks:
      - agenticx-backbone

  admin-console:
    image: agenticx-registry.cn-shanghai.cr.aliyuncs.com/agenticx/enterprise-admin-console:latest
    container_name: agenticx-admin-console
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DATABASE_DIALECT: ${DATABASE_DIALECT:-postgresql}
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL is required}
      AUTH_COOKIE_SECURE: ${AUTH_COOKIE_SECURE:-true}
      GATEWAY_BASE_URL: ${GATEWAY_BASE_URL:-http://192.168.16.66:8088}
      AUTH_JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY}
      AUTH_JWT_PRIVATE_KEY: ${JWT_PRIVATE_KEY}
      ADMIN_CONSOLE_LOGIN_PASSWORD: ${ADMIN_CONSOLE_LOGIN_PASSWORD:?ADMIN_CONSOLE_LOGIN_PASSWORD is required}
      ADMIN_CONSOLE_SESSION_SECRET: ${ADMIN_CONSOLE_SESSION_SECRET:?ADMIN_CONSOLE_SESSION_SECRET is required}
      DEFAULT_TENANT_ID: ${DEFAULT_TENANT_ID:?DEFAULT_TENANT_ID is required}
      DEFAULT_DEPT_ID: ${DEFAULT_DEPT_ID:-default}
      ENTERPRISE_ADMIN_RUNTIME_DIR: /runtime/admin
      ENTERPRISE_QUOTA_CONFIG_FILE: /runtime/admin/quotas.json
      ENTERPRISE_POLICY_SNAPSHOT_FILE: /runtime/admin/policy-snapshot.json
      ENTERPRISE_POLICY_OVERRIDE_FILE: /runtime/admin/policy-overrides.json
    volumes:
      - gateway_runtime:/runtime
      - ../../plugins:/app/plugins:ro
    networks:
      - agenticx-backbone

  postgres-primary:
    profiles: ["postgresql"]
    image: postgres:16-alpine
    container_name: agenticx-postgres-primary
    restart: unless-stopped
    environment:
      POSTGRES_DB: agenticx
      POSTGRES_USER: agenticx
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TZ: UTC
    volumes:
      - pg_primary_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agenticx -d agenticx"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - agenticx-backbone

  postgres-replica:
    profiles: ["postgresql"]
    image: postgres:16-alpine
    container_name: agenticx-postgres-replica
    restart: unless-stopped
    environment:
      POSTGRES_DB: agenticx
      POSTGRES_USER: agenticx
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TZ: UTC
      # 说明：实际主从复制参数（wal_level/primary_conninfo 等）由客户环境初始化脚本注入。
    volumes:
      - pg_replica_data:/var/lib/postgresql/data
    depends_on:
      postgres-primary:
        condition: service_healthy
    networks:
      - agenticx-backbone

  redis:
    image: redis:7-alpine
    container_name: agenticx-redis-prod
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--save", "900", "1", "300", "10"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - agenticx-backbone

networks:
  agenticx-backbone:
    driver: bridge

volumes:
  gateway_runtime:
  pg_primary_data:
  pg_replica_data:
  redis_data:
