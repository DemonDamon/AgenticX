基于下面提示词，用 [@tool:generate_image_gpt_image_2_high:GPT Image 2 High:https://lovart-persist-us.oss-us-east-1.aliyuncs.com/web/icon/chatgpt.svg] 绘制精美的中文信息图。

[主题与布局设想]

绘制一张标题为“Near Desktop 架构”的横向架构图，核心表达：

“Near 是本机优先的多智能体桌面工作台；默认链路由 Electron 桌面壳与本机 Agent Runtime 构成，同时支持已落地的远程单实例后端，Cluster / HA 仍是规划方向。”

画布采用从左到右的三段式布局：

1. 左侧：Near Desktop
2. 中部：本机 Agent Runtime，作为视觉核心
3. 右侧：外部按需能力与可选远程运行方式
4. 底部：本地数据面

使用一个较大的深墨色设备边界（细铬银描边）包裹 Near Desktop、本机 Agent Runtime 和本地数据，标题为“用户设备｜macOS / Windows；Linux 为构建目标”。

默认主链路采用带极弱蓝紫光晕的粗实线；按需调用采用细实线；已落地但非默认能力采用细虚线加明确状态文字；规划能力采用暗紫灰虚线框。全图最多两层有边框容器。

[视觉模块详解]

一、左侧“Near Desktop｜已落地”

使用深底、电光蓝实线描边（细金属高光）的圆角矩形。

内部自上而下放置：

1. “React 多窗格界面”
   - 对话 · 分身 · 群聊
   - 设置 · 历史 · 工作区

2. “Zustand 状态”
   - 窗格 · 消息 · 模型
   - 流式状态 · Token

3. “Electron 主进程”
   - 窗口与进程
   - IPC 与系统集成
   - 自动化与 Sidecar

“React 多窗格界面”与“Electron 主进程”之间使用双向细实线，标注“Preload IPC”；Zustand 作为界面内部状态层，不单独画跨系统箭头。

容器外左侧放置简洁人物图标“用户”，通过粗实线连接 Near Desktop，标注“对话 · 配置 · 工作区操作”。

二、中部“本机 Agent Runtime｜默认 · 已落地”

使用画面中面积最大的深蓝紫核心容器（边缘可有极弱电光蓝 / 紫罗兰 bloom）。不得使用“Edge Agent”命名，避免与 Enterprise Edge Agent 混淆。

顶部放置核心节点：

“agx serve / agx-server”

副标题：“Python Studio + Agent Runtime｜REST API + SSE”

核心节点下方采用两行能力网格。

第一行：

- “会话与消息”
- “Meta-Agent · 委派”
- “分身 · 群聊”
- “流式事件 · 确认”

第二行：

- “工具 · MCP”
- “Skills · Hooks”
- “记忆 · 会话检索”
- “知识库 · LLM 路由”

从“Electron 主进程”到“agx serve / agx-server”绘制粗实线双向连接：

- 正向标注：“HTTP 请求”
- 反向标注：“SSE 流式事件”
- 连接旁增加小标签：“127.0.0.1｜默认本地链路”

三、中下部“本机执行面｜已落地”

在 Runtime 容器下方放置两张并列卡片：

1. “Runtime 工具执行”
   - 文件 · Bash · LiteParse
   - MCP stdio · Knowledge Search

2. “Desktop 原生执行”
   - 内嵌终端 · node-pty
   - Computer Use · 原生连接器

从 Agent Runtime 到“Runtime 工具执行”使用粗实线，标注“工具调用 / 执行结果”。

从 Electron 主进程到“Desktop 原生执行”使用细实线，标注“IPC / 系统能力”。

不得将两个执行面合并成一个无法区分归属的节点。

四、底部“本地数据面｜已落地”

在用户设备边界底部设置深蓝灰横向区域，内部放置四个存储节点：

1. 圆柱形“本地配置”
   `~/.agenticx/config.yaml`

2. 圆柱形“会话与角色”
   `sessions · avatars · groups`

3. 圆柱形“记忆与知识库”
   `SQLite · Chroma · graph（可选）`

4. 文件形“运行数据”
   `workspace · logs · layout`

从 Agent Runtime 分别以细实线连接四个节点，统一标注“本地读写”。

从 Near Desktop 到“本地配置”和“运行数据”增加细实线，标注“设置与布局持久化”。

五、右侧上方“外部能力｜按需调用”

使用深底、电光蓝实线描边（细铬银高光）容器，置于用户设备边界之外。

内部纵向排列：

- 云形节点“兼容模型服务”
- 圆角矩形“远程 MCP Server”
- 圆角矩形“Skill / Bundle 注册表”
- 圆角矩形“飞书 / 微信等外部渠道”

从本机 Agent Runtime 分别以细实线连接：

- 模型服务：“模型请求 / 流式响应”
- MCP Server：“MCP 协议”
- 注册表：“搜索 / 安装”

从 Electron 主进程向外部渠道绘制细实线，标注“本地模式自动拉起 Sidecar / 消息同步”。

不得将 IM Sidecar 画成远程模式下默认启动。

六、右侧中部“远程后端｜可选 · 已落地”

使用深底电光蓝实线框，内部放置：

- “远程 agx serve”
- “单一 Server URL”
- “Token 鉴权”

从 Near Desktop 直接绘制一条细虚线指向该区域，标注：

“remote_server｜替代本机后端”

该连线不得穿过本机 Agent Runtime。远程后端与本机后端是二选一关系，不得画成默认双活。

七、右侧下部“Cluster / HA｜规划中”

使用低视觉权重的暗紫灰虚线容器，内部放置：

- “统一入口”
- “多副本 Agent Runtime”
- “共享会话与运行资源”

仅从“远程后端”以细虚线指向该区域，标注“未来演进”。不得接入默认粗实线主链路。

八、“扩展体验｜已落地”

在 Near Desktop 容器底部增加一条无外框能力带，使用小图标与短标签：

- “Voice Focus”
- “Automation”
- “Claude Code Bridge”
- “Code Index”
- “Data Sources”

该能力带只表达产品体验，不展开内部实现，不增加额外连线。

九、状态与连线图例

右下角放置紧凑图例：

- 电光蓝实线框：“已落地”
- 深底电光蓝实线框：“可选 · 已落地”
- 暗紫灰虚线框：“规划中”
- 粗实线（可带极弱蓝紫光晕）：“默认主链路”
- 细实线：“按需调用”
- 细虚线：“替代链路 / 未来演进”

[风格与配色方案]

采用与品牌 Logo（互锁铬金属环球）一致的「premium chrome · dark studio」体系：纯黑底、抛光铬银金属气质，电光蓝与紫罗兰 / 紫色虹彩高光、明亮白镜面高光点，整体如高端科技产品发布视觉；架构图本身保持二维矢量可读性，不得把节点画成 Hopf / Borromean 立体环结。

- 页面背景：#05060A（pure black）
- 铬银金属：#C8D0DC（描边 / 细高光）
- 品牌电光蓝：#2F7BFF
- 强调紫罗兰：#8B5CFF
- 明亮白高光点：#FFFFFF（仅边缘微量 specular）
- 镜面银正文：#E8EEF8
- 次级文字：#8B95A8（对齐 Logo 副标题灰）
- 容器深底：#12161F
- Runtime 深蓝紫底：#182033
- 设备边界：#0E121C
- 主链路：#3D8BFF（可带极弱蓝→紫光晕，勿过亮霓虹）
- 规划虚线：#6A6480

光源：左上方柔和 studio lighting，制造轻微层次与金属描边反射；禁止厚重戏剧性投影盖住文字。

允许：深黑背景、细窄抛光铬银 / 电光蓝描边、主容器边缘极弱蓝紫 bloom、主链路极弱蓝→紫高光、少量明亮白 specular 点。

禁止：品红 / 青绿作为主强调色、浅色论文风白底大面积铺色、厚重投影、写实 3D 图标堆砌、把每个模块画成立体金属环结、装饰性人物插画、绿色 / 橙色完成度色、彩虹色块喧宾夺主。节点仍为扁平圆角矩形 / 标准圆柱 / 云形。

全图主色为黑、铬银、电光蓝、紫罗兰与白高光。用边框、线型与状态文字区分能力状态。

所有节点统一圆角半径。中文节点标题不超过 12 个汉字，最多两行；卡片内最多四个短语。保持足够内边距，不得通过缩小字号容纳长文本。

主链路零交叉；次要连线使用正交折线且最多一次转折；任何箭头不得穿过文字或节点。

[技术参数建议]

文字必须清晰可辨：浅银 / 白字于深底，保持高对比。比例 16:9，输出约 2560×1440。使用标准流程图符号。暗场品牌信息图风格——扁平矢量结构 + Logo 同系 premium chrome 光感，非学术浅色论文图，亦非整幅 3D 金属雕塑。
