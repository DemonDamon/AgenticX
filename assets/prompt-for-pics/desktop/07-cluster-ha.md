GPT Image 2 High — LOCAL REGION PROMPT: CLUSTER / HA

This is a self-contained English prompt for rendering one local region of the “Near Desktop Architecture” infographic with GPT Image 2 High. Draw only this region and its short interface-port stub.

# Scope

Render **“Cluster / HA | Planned”** as a low-emphasis future direction evolved only from the implemented Remote Backend.

# Canvas placement

- Full composition: 2560 × 1440, 16:9.
- Region bounding box: x 75–96, y 66–82, outside the User Device.
- Keep x 69–74 completely clear as a master routing corridor.
- Use a gray-blue dashed container with planned-state line color `#8CAAB8`.

# Local nodes

- **Unified Endpoint**
- **Multi-replica Agent Runtime**
- **Shared Session and Runtime Resources**

# Internal layout

- Stack the three planned nodes vertically.
- Use low-emphasis white or very pale gray-blue fills.
- Keep the region visually subordinate to Local Agent Runtime and Remote Backend.

# Internal connections

- Do not invent replication, load-balancing, or database arrows.
- Use containment to communicate the planned grouping.

# Interface ports

Draw only a short stub; do not draw the complete cross-region line.

- `HA.IN.REMOTE_EVOLUTION`: top boundary, x 84, y 66; thin dashed downward-facing stub.

# Do not draw

- Do not draw Remote Backend, Desktop, Local Agent Runtime, or local storage.
- Do not complete the Future Evolution connection.
- Do not connect this region to the default thick solid path.
- Do not mark any HA element as implemented.
- Do not add products, orchestrators, databases, or topology details.

# Rendering constraints

- Flat vector style, crisp English text, high contrast without visual dominance.
- Planned semantics must use gray-blue dashed frames.
- Use only blue/cyan-blue/gray-blue/white.
- No gradients, neon, glass, 3D, heavy shadows, or decorative illustrations.
