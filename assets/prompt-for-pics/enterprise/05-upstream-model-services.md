GPT Image 2 High — Local Region Prompt: Upstream Compatible Model Services

Use this prompt directly with GPT Image 2 High to render only the external upstream-model-services region. Do not rely on any chat context.

# Scope

Render “Upstream Compatible Model Services | External” as the external destination for Gateway model requests and streaming responses.

# Canvas placement

Use normalized full-canvas bounds `x=78–96, y=47–58`, exactly matching `00-master.md`. This cloud is outside the private deployment boundary, above the evolution region `y=61–81`, and aligned with Gateway model request/response lanes at `y=52/56`. Keep the outer-right gutter `x=96–97` clear.

# Local nodes

Inside one simple cloud outline:

- “OpenAI-compatible API”
- “Enterprise-dedicated Model Service”
- “Other Compatible Providers”

# Internal layout

Use a clean cloud outline with three evenly stacked or evenly spaced text items. Keep the cloud simple, white-filled, and compact enough to leave visible separation from both the private boundary and evolution area.

# Internal connections

Do not draw internal arrows among the three service labels. They are peer examples of compatible upstream services.

# Interface ports

- `MOD-REQ-IN`: border anchor `(78,52)`, corridor-facing nominal coordinate `(77,52)`; thick solid inbound request stub.
- `MOD-RESP-OUT`: border anchor `(78,56)`, corridor-facing nominal coordinate `(77,56)`; thick solid outbound response stub parallel to `MOD-REQ-IN`.

Show only short labeled stubs. The master prompt completes the model request and response routes.

# Do not draw

- Do not place this cloud inside the Enterprise Private Deployment Domain.
- Do not add verified or unverified vendor logos, proprietary logos, or specific vendor names beyond the generic “OpenAI-compatible API” protocol label.
- Do not connect this cloud directly to Web Portal, Control Plane, Edge Agent, Cluster Runtime, or databases.
- Do not complete the cross-region request or response arrows.

# Rendering constraints

Use a white fill and solid blue external frame, deep-blue text, and the global restrained palette. Use one standard simple cloud outline, crisp high-contrast English text, and parallel request/response port alignment. Flat vector only; no gradients, glow, glassmorphism, 3D, complex illustration, or heavy shadow.
