This is a local-region prompt for GPT Image 2 High. Copy and use it directly to render the Open Protocol and Interaction Ecosystem region of the AgenticX architecture diagram. It is self-contained and does not depend on any chat context.

# Open Protocol and Interaction Ecosystem

## Scope

Render only the external/pluggable protocol ecosystem on the left side.

## Canvas placement

Place a tall white container at x 2–16 and y 27–63 of the final 16:9 canvas. Maintain a clear right-side gutter toward the central framework.

## Local nodes

Stack these four nodes:

1. **A2A | Agent Interoperability**
2. **MCP | Tools and Resources**
3. **AG-UI | Streaming Interaction**
4. **REST / SSE / WebSocket**

## Internal layout

Use one vertical stack with equal-width compact rows, generous spacing, and a solid blue outer border. Keep the container visually secondary to the central framework.

## Internal connections

Connect the four local rows to a single internal vertical collector using short thin solid horizontal lines. The collector terminates at the east interface port. Do not expose four separate external lines.

## Interface ports

- `PROTOCOL.E.CORE`: center of the east edge; one short thin solid rightward stub; reserved assembly label **“Protocol Port”**.

## Do not draw

- Do not draw the complete line into the central framework.
- Do not route separate protocol lines into central internal nodes.
- Do not include models, tools, domain extensions, or central runtime modules.
- Do not add vendor logos.

## Rendering constraints

Use external/pluggable styling: white fill, solid `#1677C8` border, `#174A6E` titles, `#203642` body text. Flat vector, consistent rounded rectangles, no gradients or heavy shadows. All local lines are thin, solid, orthogonal, and have at most one bend. No line crosses text.
